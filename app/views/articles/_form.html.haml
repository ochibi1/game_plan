= form_for article do |f|
  .form_group
  = f.attachment_field :img
  .form_group
  = f.label :title, "title", style: "margin-top: 10px;"
  = f.text_field :title, class: 'form_control'
  .form_group
  = f.label :body, "text"
  = f.text_area :body, rows: 10, class: 'form_control mt-1'
  %br
  - if @article.blank?
    = f.submit "投稿を編集する", class: "btn btn-primary"
  - else 
    = f.submit "新規作成", class: "btn btn-primary"